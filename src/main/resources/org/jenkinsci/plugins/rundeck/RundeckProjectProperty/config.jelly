<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler" xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson" xmlns:f="/lib/form">
  <j:if test="${descriptor.sites.size() gt 1}">
    <!-- show the selection only when there's something to select -->
    <f:entry title="Rundeck site">
      <select class="setting-input" name="rundeck.siteName">
        <j:forEach var="s" items="${descriptor.sites}">
          <f:option selected="${s.siteName==instance.siteName}">${s.siteName}</f:option>
        </j:forEach>
      </select>
    </f:entry>
  </j:if>
</j:jelly>